// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/luckysxx/go-note/internal/ent/share"
	"github.com/luckysxx/go-note/internal/ent/snippet"
)

// Share is the model entity for the Share schema.
type Share struct {
	config `json:"-"`
	// ID of the ent.
	// 雪花算法生成的全局唯一 ID
	ID int64 `json:"id,omitempty"`
	// 公开分享 token
	Token string `json:"token,omitempty"`
	// 分享类型：article=文章 template=模板
	Kind share.Kind `json:"kind,omitempty"`
	// 关联的文档 ID
	SnippetID int64 `json:"snippet_id,omitempty"`
	// 分享创建者 ID
	OwnerID int64 `json:"owner_id,omitempty"`
	// 可选的密码 hash
	PasswordHash string `json:"password_hash,omitempty"`
	// 可选的过期时间
	ExpiresAt *time.Time `json:"expires_at,omitempty"`
	// 浏览次数
	ViewCount int `json:"view_count,omitempty"`
	// 模板 Fork 次数
	ForkCount int `json:"fork_count,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ShareQuery when eager-loading is set.
	Edges        ShareEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ShareEdges holds the relations/edges for other nodes in the graph.
type ShareEdges struct {
	// Snippet holds the value of the snippet edge.
	Snippet *Snippet `json:"snippet,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// SnippetOrErr returns the Snippet value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ShareEdges) SnippetOrErr() (*Snippet, error) {
	if e.Snippet != nil {
		return e.Snippet, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: snippet.Label}
	}
	return nil, &NotLoadedError{edge: "snippet"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Share) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case share.FieldID, share.FieldSnippetID, share.FieldOwnerID, share.FieldViewCount, share.FieldForkCount:
			values[i] = new(sql.NullInt64)
		case share.FieldToken, share.FieldKind, share.FieldPasswordHash:
			values[i] = new(sql.NullString)
		case share.FieldExpiresAt, share.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Share fields.
func (_m *Share) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case share.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int64(value.Int64)
		case share.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				_m.Token = value.String
			}
		case share.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				_m.Kind = share.Kind(value.String)
			}
		case share.FieldSnippetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field snippet_id", values[i])
			} else if value.Valid {
				_m.SnippetID = value.Int64
			}
		case share.FieldOwnerID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field owner_id", values[i])
			} else if value.Valid {
				_m.OwnerID = value.Int64
			}
		case share.FieldPasswordHash:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password_hash", values[i])
			} else if value.Valid {
				_m.PasswordHash = value.String
			}
		case share.FieldExpiresAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expires_at", values[i])
			} else if value.Valid {
				_m.ExpiresAt = new(time.Time)
				*_m.ExpiresAt = value.Time
			}
		case share.FieldViewCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field view_count", values[i])
			} else if value.Valid {
				_m.ViewCount = int(value.Int64)
			}
		case share.FieldForkCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field fork_count", values[i])
			} else if value.Valid {
				_m.ForkCount = int(value.Int64)
			}
		case share.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Share.
// This includes values selected through modifiers, order, etc.
func (_m *Share) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySnippet queries the "snippet" edge of the Share entity.
func (_m *Share) QuerySnippet() *SnippetQuery {
	return NewShareClient(_m.config).QuerySnippet(_m)
}

// Update returns a builder for updating this Share.
// Note that you need to call Share.Unwrap() before calling this method if this Share
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Share) Update() *ShareUpdateOne {
	return NewShareClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Share entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Share) Unwrap() *Share {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Share is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Share) String() string {
	var builder strings.Builder
	builder.WriteString("Share(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("token=")
	builder.WriteString(_m.Token)
	builder.WriteString(", ")
	builder.WriteString("kind=")
	builder.WriteString(fmt.Sprintf("%v", _m.Kind))
	builder.WriteString(", ")
	builder.WriteString("snippet_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SnippetID))
	builder.WriteString(", ")
	builder.WriteString("owner_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.OwnerID))
	builder.WriteString(", ")
	builder.WriteString("password_hash=")
	builder.WriteString(_m.PasswordHash)
	builder.WriteString(", ")
	if v := _m.ExpiresAt; v != nil {
		builder.WriteString("expires_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("view_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.ViewCount))
	builder.WriteString(", ")
	builder.WriteString("fork_count=")
	builder.WriteString(fmt.Sprintf("%v", _m.ForkCount))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Shares is a parsable slice of Share.
type Shares []*Share
